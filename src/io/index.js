const custom_stream = require('./stream/custom_stream');
const custom_reader = require('./stream/custom_reader');
const custom_writer = require('./stream/custom_writer');

module.exports = {
    custom_stream,
    custom_reader,
    custom_writer
}